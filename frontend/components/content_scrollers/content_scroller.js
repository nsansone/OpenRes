import React from 'react';
import { PopRestItem } from '/pop_rest_item';

export default class ContentScroller extends React.Component {
    render() {
        const restsLis = this.props.contentType === 'rests' ?
        (this.props.rests.map) 
    }
}